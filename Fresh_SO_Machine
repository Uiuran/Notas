verify drivers with lspci
sudo apt-get update
install git
install byobu
install vim-gtk3
install pyenv
curl https://pyenv.run | bash
add to .bashrc
export PYENV_ROOT="$HOME/.pyenv"
command -v pyenv >/dev/null || export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"
install dbeaver
